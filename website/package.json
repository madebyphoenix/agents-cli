{
  "name": "@phnx-labs/agents-cli-landing",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker serving agents-cli.sh (UA-sniffed HTML landing + bash installer).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "build": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.6.0",
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "wrangler": "^4.38.0"
  }
}
